# Optional recognition-model endpoints consumed by the vision pipeline.
# Leave unset to run geometry-only (still fully functional).
VITE_DETECTOR_MODEL_URL=
VITE_CLASSIFIER_MODEL_URL=
VITE_SEGMENTER_MODEL_URL=
# Override the OpenCV.js bundle location if self-hosting.
VITE_OPENCV_URL=
